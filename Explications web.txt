API is a piece of code that listens to a port

MongoDB is the database. It is wether stored locally or in a docker container or on MongoDB-Atlas (free online)
A client like Insomnia or Postman is just a visual product that allows us to see things clearly compared to working on the cmd
Deploy the API on a serverless cloud: corresponds to running the code online*


All this procedure is meant to put the data and the API online so someone manages it for us 
Everything is online so there is no mean to disconnect at any moment

MongoDb Atlas credentials : CRohart / capu